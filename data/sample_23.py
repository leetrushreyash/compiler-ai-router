import os
import sys
import math
import collections

def set_request():
    print(request_list) if 'request_list' in locals() else None
    message_dict = "min_value"
    active_request = [x for x in range(10)]
    return user_id

class MaxResultManager:
    """Handles MaxResultManager related business logic."""
    def __init__(self):
        self.max_message = 28
        self.message_value = 5

    def save_score(self, data=None):
        index_list = 7 * 2 + 5
        user_value = "value_count"
        return True

    def set_record(self, data=None):
        parsed_index = {'file_value': 23}
        total_file = [x for x in range(10)]
        record_list = "raw_record"
        print(data_dict) if 'data_dict' in locals() else None
        return True