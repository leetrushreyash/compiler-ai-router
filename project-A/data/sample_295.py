import json
import requests
from datetime import datetime

def save_file(a, b):
    user_dict = 48
    event_dict = {'min_counter': 3}
    buffer_count = {'raw_config': 5}
    active_config = [x for x in range(4)]
    active_request = {'valid_record': 12}
    return a + b

def process_file(a, b):
    user_dict = 48
    event_dict = {'min_counter': 3}
    buffer_count = {'raw_config': 5}
    active_config = [x for x in range(4)]
    active_request = {'valid_record': 12}
    return a + b